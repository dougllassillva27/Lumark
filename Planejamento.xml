<plan>
  <task>
    <name>Inicializar projeto Vite React</name>
    <files>package.json, vite.config.js, index.html, src/main.jsx, src/App.jsx</files>
    <action>Executar scaffold do Vite React + JS, adicionar dependências acordadas (zustand, lucide-react, dompurify, markdown-it), limpar boilerplate não utilizado.</action>
    <verify>Servidor dev sobe. Dependências instalam sem conflito. App.jsx renderiza estado inicial limpo.</verify>
  </task>
  
  <task>
    <name>Criar estrutura de pastas e arquivos-base</name>
    <files>src/components/, src/lib/, src/store/, src/styles/, src/constants/, src/hooks/</files>
    <action>Criar hierarquia de pastas exata baseada no plano técnico V1, criando arquivos estruturais vazios ou exportações mínimas para o layout.</action>
    <verify>Estrutura em disco bate 100% com Documento de Plano Técnico (Item 7).</verify>
  </task>
  
  <task>
    <name>Implementar tema AMOLED base</name>
    <files>src/styles/base.css, src/styles/theme.css, src/styles/layout.css, src/main.jsx</files>
    <action>Definir variáveis CSS globais (tokens de cor e tipografia), aplicar reset CSS focado em leitor, excluir CSS padrão do Vite (App.css/index.css).</action>
    <verify>Página exibe fundo escuro (#000000) e superfície AMOLED (#0b0b0b) testáveis visualmente pelo App.jsx montado.</verify>
  </task>
</plan>

<plan>
  <task>
    <name>Construir layout leitor-first</name>
    <files>src/components/Layout.jsx, src/components/Header.jsx, src/components/EmptyState.jsx, src/App.jsx</files>
    <action>Montar o shell da aplicação com Header fixo e um estado inicial (EmptyState) que comunique as 3 formas de entrada de conteúdo. O App.jsx orquestrará a exibição.</action>
    <verify>Tela vazia renderiza o layout principal e o componente EmptyState, sem funcionalidade ainda.</verify>
  </task>
  <task>
    <name>Construir modos de visualização</name>
    <files>src/components/ViewSwitcher.jsx, src/components/EditorPane.jsx, src/components/PreviewPane.jsx, src/store/useDocumentoStore.js</files>
    <action>Criar os componentes de painel (Editor e Preview) e o alternador de modo (ViewSwitcher). Adicionar `modoVisualizacao` na store Zustand para controlar a exibição.</action>
    <verify>Alternar modo no ViewSwitcher atualiza o estado na store e exibe/oculta os painéis correspondentes (com conteúdo mockado).</verify>
  </task>
</plan>

<plan>
  <task>
    <name>Implementar upload e validação de arquivo</name>
    <files>src/components/FileDropzone.jsx, src/lib/arquivo.js, src/lib/validacao.js</files>
    <action>Ler .md/.txt por seletor e drag-and-drop com validações</action>
    <verify>Arquivos válidos carregam e inválidos geram erro amigável</verify>
  </task>

  <task>
    <name>Implementar colar texto</name>
    <files>src/components/PasteModal.jsx, src/store/useDocumentoStore.js</files>
    <action>Criar fluxo de colagem com criação de documento sem título</action>
    <verify>Texto colado aparece no estado e na interface</verify>
  </task>
</plan>

<plan>
  <task>
    <name>Implementar parser markdown</name>
    <files>src/lib/markdown.js</files>
    <action>Instanciar e configurar markdown-it para renderização de strings Markdown em HTML bruto.</action>
    <verify>Headings, listas, tabelas e code blocks são convertidos para tags HTML correspondentes.</verify>
  </task>

  <task>
    <name>Implementar sanitização do HTML</name>
    <files>src/lib/sanitizacao.js, src/components/PreviewPane.jsx</files>
    <action>Configurar DOMPurify em sanitizacao.js. Importar e aplicar o parser e a sanitização no PreviewPane.jsx para renderizar o HTML seguro via dangerouslySetInnerHTML.</action>
    <verify>Tags &lt;script&gt; e handlers inline (onload, etc) do documento ativo são removidos antes da injeção no DOM.</verify>
  </task>

  <task>
    <name>Aplicar estilo de documentação ao preview</name>
    <files>src/styles/markdown.css, src/main.jsx, src/components/PreviewPane.jsx</files>
    <action>Criar e importar markdown.css com reset e tipografia ricas (tabelas, blockquotes, code blocks) sob uma classe pai (ex: .markdown-body). Aplicar classe no PreviewPane.</action>
    <verify>Preview exibe conteúdo estruturado, responsivo e aderente ao tema AMOLED.</verify>
  </task>
</plan>
